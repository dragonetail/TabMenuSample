# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TabMenu' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TabMenu
  #pod 'ImageIOSwift_F2', :path => '../LocalPods/ImageIOSwift_F2/'
  #pod 'SwipeSelectingCollectionView2', :path => '../LocalPods/SwipeSelectingCollectionView2/'
  #pod 'SwiftPagingTabView', :path => '../LocalPods/SwiftPagingTabView/'
  pod 'SwiftBaseBootstrap', :path => '../LocalPods/SwiftBaseBootstrap/'
  
  #pod 'GRDB.swift', :path => '../LocalPods/GRDB.swift/'
  pod 'PureLayout', :path => '../LocalPods/PureLayout/'
  pod 'XCGLogger', :path => '../LocalPods/XCGLogger/'
  #pod 'ObjcExceptionBridging', :path => '../LocalPods/XCGLogger/'
  #pod 'FLEX', :configurations => ['Debug'], :path => '../LocalPods/FLEX/'
  #pod 'Eureka', :path => '../LocalPods/Eureka/'
  #pod 'SwiftyJSON', :path => '../LocalPods/SwiftyJSON/'
  #pod 'GesturePassword', :path => '../LocalPods/GesturePassword/'
  #pod 'DataCompression', :path => '../LocalPods/DataCompression/'
  #pod 'FAPanels', :path => '../LocalPods/FAPanels/'
  #pod 'SwiftEventBus', :path => '../LocalPods/SwiftEventBus/'
  #pod 'CyaneaOctopus', :path => '../LocalPods/CyaneaOctopus/'

end
